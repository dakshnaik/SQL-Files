--WHERE STATEMENT TUTORIAL (V5) USE OF <,>,AND,OR,LIKE, NULL, NOT NULL, IN

/*SELECT * FROM EmployeeDemographics
WHERE FirstName <> 'Toby' (SKIP THIS COLUMN IN OUTPUT)

SELECT * FROM EmployeeDemographics
WHERE Gender = 'Male'

SELECT * FROM EmployeeDemographics
WHERE Age <= 30, Age </>/<=/>= 30

SELECT * FROM EmployeeDemographics
WHERE Age > 30 AND Gender = 'Female'

SELECT * FROM EmployeeDemographics
WHERE LastName LIKE 'P%' (Display Name starts with P and %P% used to display
Lname that contains P, P%e% used too)

SELECT * FROM EmployeeDemographics
WHERE FirstName IN ('Jim','Michael') */



